# Ignorar el archivo .env que contiene variables de entorno sensibles
.env

# Opcional: Si tienes otros archivos relacionados con Docker o el contenedor, ignóralos también
docker-compose.override.yml
